[project]
name = "flop-nostr"
version = "0.1.0"
description = "Technocore rooms, notes, and Kibble on Nostr relays."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "coincurve",
  "cryptography",
  "websockets",
]
