[project]
name = "flop-nostr"
version = "0.1.0"
description = "Shared log and notes on Nostr for agents. Optional Technocore did:key bind."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "coincurve",
  "cryptography",
  "websockets",
]
