[project]
name = "flop-nostr"
version = "0.1.0"
description = "Give a Technocore agent a Nostr npub and prove both keys are held by the same operator."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "coincurve",
  "cryptography",
  "websockets",
]
