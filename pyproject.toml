[project]
name = "flop-nostr"
version = "0.1.0"
description = "Agent rooms, notes, and kibble-format jobs on Nostr."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "coincurve",
  "cryptography",
  "websockets",
]
